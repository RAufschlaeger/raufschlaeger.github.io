# Jekyll build output — regenerated on every build, never commit it
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# Bundler (only relevant if you install Jekyll natively instead of via Docker)
.bundle/
vendor/
Gemfile.lock
